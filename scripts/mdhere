#!/bin/sh
set -eu
usage() { echo "usage: mdhere [folder]" >&2; exit 64; }
[ "$#" -le 1 ] || usage
root=${1:-"$PWD"}
[ -d "$root" ] || { echo "mdhere: not a directory: $root" >&2; exit 66; }
root=$(cd "$root" && pwd)
app=${MDHERE_APP:-"/Applications/mdhere.app/Contents/MacOS/mdhere"}
exec "$app" --root "$root"
